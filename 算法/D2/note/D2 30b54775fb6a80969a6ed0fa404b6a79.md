# D2

上次编辑时间: 2026年2月24日 22:00
状态: 进行中

# PLAN

- [x]  学会使用 `string` 处理字符串类型题目
- [x]  使用 `vector`  处理数组题目
- [x]  学会使用 for(auto &x：v)常用遍历写法

## addition

- 完成基本算法笔记（）：
    - [ ]  位运算
    - [ ]  枚举、模拟、递推
    - [ ]  二分
    - [ ]  排序
    - [ ]  倍增
    - [ ]  贪心

---

# NOTES

## PART A

### 语法学习

1. 模板引入 - using ll == long long;
    
    ```cpp
    # include <bits/stdc++.h>
    using namespace;
    using ll = long long;
    
    int main(){
    	ios::sync_with_wtdio(false);
    	cin.tie(nullptr);
    	/*
    	codes....
    	*/
    	return 0;
    ```
    
2. string 的基本用法
    - 定义与输出
        
        ```cpp
        string s = "hello";
        cin >> s;
        ```
        
    - 常用函数与操作
        
        ```cpp
        int n = s.size();
        char c = s[0];
        s += "world";
        string t = s.substr(1,3) // 从下标1开始，取长度为3的字符
        ```
        
        <aside>
        💡
        
        多行注释快捷方法（VS Code 默认）：
        
        - 块注释可用 `Shift + Alt + A`。
        - 选中多行后按 `Ctrl + /` 进行行注释切换（Windows）。
        </aside>
        
    - 遍历字符串
        
        ```cpp
        for (int i = 0; i < (int)s.size(); i++) {
            cout << s[i] << "\n";
        }
        
        for (char c : s) {
            cout << c << "\n";
        }
        ```
        
3. vector 基本用法
    - 定义与初始化
        
        ```cpp
        vector<int> v; // 为空
        vector<int> v2(5); // 长度为5，元素为0
        vector<int> v3(5,7); // 长度为5，元素为7
        vector<int> v4(1,2,3); // 初始化列表
        ```
        
    - 常见操作
        
        ```cpp
        v.push_back(10); // 在末尾添加元素10
        v.pop_back(); //删除末尾元素
        int x = v[]; // 通过下标访问元素
        int n = v.size(); // 输出vector的大小
        c.clear(); // 清空vector中的所有元素
        ```
        

### **练习：**

1. **手写一个反转字符串函数**
    
    ```cpp
    string reverseString(string s) {
        int n = s.size();
        for (int i = 0, j = n - 1; i < j; i++, j--) {
            swap(s[i], s[j]);
        }
        return s;
    }
    ```
    
2. **判断字符串是否为回文**
    
    ```cpp
    bool isPalindrome(string s) {
        int i = 0, j = (int)s.size() - 1;
        while (i < j) {
            if (s[i] != s[j]) return false;
            i++;
            j--;
        }
        return true;
    }
    ```
    
3. **求一组数的最大值、最小值、平均值**
    
    ```cpp
    double average(vector<int> &v) {
        ll sum = 0;
        for (int x : v) sum += x;
        return (double)sum / v.size();
    }
    ```
    
4. **将数组中所有元素 +1**
    
    ```cpp
    void addOne(vector<int> &v) {
        for (auto &x : v) {
            x++;
        }
    }
    ```
    

---

## addition