# codespce

Simple C++ example that prints a message and a tiny test runner.

## Build

Use CMake to configure and build:

```powershell
cmake -S . -B cmake-build-debug
cmake --build cmake-build-debug
```

## Run

```powershell
.\cmake-build-debug\codespce.exe
.\cmake-build-debug\codespce_test.exe
```

